# Pin the control-node toolchain for reproducible runs (asdf / mise compatible).
# Install ansible-core matching this, then: ansible-galaxy collection install -r requirements.yml
python 3.12.7
# ansible-core is installed via pip; pin it in your venv:
#   pip install "ansible-core>=2.17,<2.19"
