---
- name: Provision Raspberry Pi home server
  hosts: all
  become: true
  gather_facts: true

  vars_files:
    - "{{ playbook_dir }}/vault.yml"

  roles:
    - role: base
    - role: docker
    - role: home_server
