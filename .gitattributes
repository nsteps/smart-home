# GitHub Linguist language detection.
# This is an Ansible/YAML project; Jinja2 templates render YAML/conf, so count
# them as YAML instead of letting them skew the repo's primary language.
# YAML is a "data" language, which Linguist excludes from the stats by default,
# so force it detectable — otherwise the language bar shows nothing at all.
*.j2 linguist-language=YAML linguist-detectable=true
*.yml linguist-language=YAML linguist-detectable=true
*.yaml linguist-language=YAML linguist-detectable=true

# Keep meta files out of the language statistics.
*.cfg linguist-language=INI
molecule/** linguist-detectable=false
.github/** linguist-detectable=false
